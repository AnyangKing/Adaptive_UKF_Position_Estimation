# 실험 감사 체크리스트

작성일: 2026-08-11  
목적: Adaptive UKF 수중 USBL 위치추정 연구를 논문 작성 단계의 방어 문장으로 보완하는 것이 아니라, 실험 수행 단계부터 실제 획득 가능한 관측량과 공정한 검증 기준으로 관리한다.

## 1. 고정 원칙

### 1.1 정답 누설 금지

- ground-truth 위치는 수신 신호 합성과 최종 오차 계산에만 사용한다.
- TOA, TDOA, DOA, 관측 신뢰도, adaptive-R, carrier schedule/guard 결정에는 ground truth를 사용하지 않는다.
- DOA는 센서별 정답 기하각이 아니라 다채널 수신 신호에서 추출한 하나의 array-level DOA만 사용한다.
- UKF sigma point에서 `h(x)`를 계산하는 것은 정상적인 관측 모델이므로 정답 누설로 보지 않는다.
- truth-derived residual은 offline mechanism diagnosis에만 쓸 수 있고, 제안법 입력 또는 threshold tuning에 쓰면 안 된다.

### 1.2 실제 획득 가능한 관측만 사용

채택 실험의 기본 관측 pipeline은 다음으로 제한한다.

- reference TOA: matched-filter peak 기반
- reference TDOA: GCC-PHAT 기반 pair delay를 reference sensor 기준 7개 range-difference로 변환
- DOA: 5 ms direct-path gate 안의 SRP-PHAT array-level azimuth/elevation
- quality: peak margin, GCC-SRP disagreement, block/total NIS처럼 수신 신호와 필터 내부량에서 계산 가능한 값
- oracle feature 또는 ground-truth label은 성능 상한/기전 진단에만 사용한다.

### 1.3 USBL 수신 프로토콜 가정

현재 채택 코드의 기본 가정은 다음으로 문서화한다.

- 송신원: 위치추정 대상이 능동 송신하는 synchronized beacon에 해당한다.
- TOA: one-way absolute TOA를 range로 환산한다.
- 동기화: 송신 시각과 수신기 기준 clock이 공유되거나, one-way TOA offset이 보정되어 있다고 가정한다.
- 현재 canonical validation에는 explicit common clock bias, sensor별 hardware delay, sensor별 gain/phase mismatch가 포함되어 있지 않다.
- 센서별 hardware/channel delay를 포함하는 새 실험은 별도 실험으로 분리하고, 기존 canonical validation 수치와 섞지 않는다.

### 1.4 공정 비교

- 같은 수신 신호, 궤적, 환경, noise seed를 비교 대상에 입력한다.
- 최소 비교 축은 목적에 따라 구분한다.
  - 필터 비교: NLS, EKF, plain UKF, adaptive-R UKF
  - carrier schedule 비교: fixed carrier, canonical linear20 hop, 후보 schedule
- 제안법에만 별도 초기값, 별도 관측, 별도 R/Q, post-hoc gate를 제공하지 않는다.
- offline smoother나 oracle 선택은 online causal filter 결과와 분리해 보고한다.

### 1.5 데이터 분리

- development, validation, final/independent test seed를 분리한다.
- test 결과를 본 뒤 threshold, schedule, R/Q, gate를 수정하면 그 test는 development로 강등한다.
- pilot 결과는 독립 seed 대규모 검증 전까지 논문 성능 claim에 사용하지 않는다.
- 결과 파일에는 가능한 한 seed roots, trajectory/channel/noise seeds, 환경 파라미터, carrier schedule, 코드 버전 또는 근거 폴더를 저장한다.

### 1.6 주장 우선순위

- 실험 증거가 claim보다 우선이다.
- frequency agility 자체를 최초 발명했다고 주장하지 않는다.
- 현재 강한 claim은 “얕은 수중 compact USBL에서 post-gating coherent multipath DOA bias의 시간상관을 carrier-agile 관측 설계가 변화시키고, static/quasi-static long-range 조건에서 성능 개선이 재현된다”이다.
- 이동 표적 일반 RMSE 개선은 현재 재현되지 않았으므로 claim 금지.
- 실패·한계 지도와 실험 레지스트리는 새 결과가 기존 경계를 바꿀 때만 갱신한다.

## 2. 현재 프로젝트 감사 결과

| 항목 | 현재 상태 | 근거 | 부족한 점 |
|---|---|---|---|
| 정답 누설 방지 | 대체로 충족 | 61/63/82/160의 관측은 `received`에서 추출되고 truth는 합성·오차·offline diagnosis에 쓰임 | 63의 truth-derived elevation residual 사용 목적을 명시해야 함 |
| 실제 관측 사용 | 충족 | `peak_measurement.extract_measurement()`가 matched filter, GCC-PHAT, gated SRP-PHAT 사용 | 초기 폴더의 ideal 관측 결과와 채택 결과를 혼동하지 않게 해야 함 |
| USBL 프로토콜 | 보완 필요 | 코드상 one-way synchronized beacon 가정 | beacon/transponder, clock bias, sensor delay 가정 문서화 필요 |
| 소형 배열/신호 모델 | 부분 충족 | 8센서 33 mm 반경, 79 mm 수직 간격, 192 kHz, LFM, 3-path channel, colored noise | sensor hardware mismatch, common clock bias, standardized metadata 부족 |
| 비교 공정성 | 부분 충족 | 43 NLS/EKF/UKF, 44/46 plain vs adaptive-R, 61/63/82 fixed vs hop paired | 최종 논문용 비교군/튜닝 범위 표준표 필요 |
| 인과성 | 충족 | UKF는 현재/과거 ping만 사용 | 관측 추출 시간과 UKF update runtime 분리 측정 부족 |
| 재현성 | 부분 충족 | 61/82/160은 독립 seed 및 통계 기록, 162는 pilot claim 금지 | 63 JSON 복구 필요, 일부 결과에 seed root/P90/code version 누락 |
| 실험 설계 | 부분 충족 | 정지/준정지/이동, radial/tangential/vertical 일부 구분 | instantaneous range, radial/cross/vertical error, OOD motion 표준 저장 부족 |
| 기여 검증 | 부분 충족 | adaptive-R ablation, frequency agility paired validation, sparse schedule failure 기록 | GCC-SRP routing, block NIS, TOA/TDOA routing 독립 ablation은 더 정리 가능 |
| 증거 우선 주장 | 충족 | 63/160/162에서 실패·pilot claim 경계를 명시 | 새 결과마다 동일한 claim boundary 표를 붙일 것 |

## 3. 새 실험 시작 전 체크

새 numbered 실험 폴더를 만들기 전 README 또는 protocol 문서에 아래 항목을 명시한다.

- [ ] 이 실험의 목적은 성능 claim, 기전 진단, pilot, 독립검증 중 무엇인가?
- [ ] 사용 seed가 development/validation/test 중 어디에 속하는가?
- [ ] ground truth가 measurement/adaptive decision에 들어가는 경로가 없는가?
- [ ] 관측은 matched-filter TOA, GCC-PHAT TDOA, gated SRP-PHAT DOA 또는 실제 획득 가능한 quality인가?
- [ ] oracle feature를 쓰는 경우 성능 상한/진단으로만 표시했는가?
- [ ] fixed/proposed 비교가 같은 trajectory, environment, ping seed를 공유하는 paired design인가?
- [ ] threshold, carrier schedule, R/Q, gate rule이 실행 전에 동결되었는가?
- [ ] online filtering과 offline smoothing/oracle/post-hoc analysis를 분리했는가?
- [ ] 결과 JSON에 seed roots, channel params, schedule, n, settled window, metric definitions가 들어가는가?

## 4. 새 실험 종료 후 체크

- [ ] 평균 RMSE뿐 아니라 median, P90 또는 tail metric, divergence rate를 기록했는가?
- [ ] paired bootstrap confidence interval과 paired test를 기록했는가?
- [ ] 결과를 본 뒤 rule을 바꾸었다면 해당 결과를 development/pilot으로 강등했는가?
- [ ] 어떤 claim을 지지하고 어떤 claim은 지지하지 않는지 명시했는가?
- [ ] 실패한 결과를 실패_한계_지도.md에 추가할 기준에 해당하는지 판단했는가?
- [ ] 실험_레지스트리.md에 상태와 핵심 결론을 추가했는가?
- [ ] 논문 원고에 없는 결과를 practical USBL performance로 부르지 않았는가?

## 5. 현재 논문에 써도 되는 claim

- TOA/TDOA/DOA를 실제 수신 신호 기반 관측으로 추출해 UKF 계열 필터에 융합했다.
- conditional adaptive-R routing은 plain UKF 대비 일부 조건에서 안정성과 일관성을 개선했다.
- compact USBL 장거리 오차의 주요 병목은 단순 Gaussian noise가 아니라 post-gating coherent multipath DOA bias이다.
- ping-to-ping carrier agility는 fixed carrier에서 반복되는 coherent DOA residual의 시간상관을 줄인다.
- static 600 m 및 very-slow quasi-static 조건에서는 fixed carrier 대비 RMSE 개선이 재현됐다.
- moving target에서는 residual lag-1 whitening은 확인됐지만, pooled RMSE 개선은 재현되지 않았다.
- sparse/four-carrier schedule과 TOA transition guard pilot은 현재 claim이 아니라 실패/후속 연구 근거다.

## 6. 현재 논문에 쓰면 안 되는 claim

- frequency hopping USBL 또는 frequency agility 자체를 최초로 발명했다.
- carrier agility가 모든 거리, 모든 기하, 모든 moving target에서 RMSE를 개선한다.
- lag-1 autocorrelation 감소가 full whitening, Gaussianity, independence, WSS를 증명한다.
- oracle/post-hoc/pilot 결과를 제안법 성능으로 사용한다.
- 현재 canonical simulation 결과를 clock bias, sensor hardware delay, rough sea, higher-order multipath까지 포함한 practical USBL 성능으로 일반화한다.

## 7. 다음 보완 우선순위

1. 63번 moving validation JSON 복구.
2. USBL protocol assumption을 루트 문서와 원고 Method에 반영.
3. 채택 실험별 truth-leak audit 표 작성.
4. 결과 metadata 표준 키를 다음 실험부터 강제.
5. 필요할 때만 runtime 분리 측정 및 요소별 ablation을 추가한다.

## 8. 채택 실험별 truth-leak audit

| 실험 | 관측 생성 | adaptive decision 입력 | truth 사용 위치 | 판정 |
|---|---|---|---|---|
| 43. EKF/UKF/NLS 비교 | 수신 신호에서 TOA/TDOA/DOA 추출 | 없음 또는 고정 R | trajectory 생성, RMSE, NEES | 통과. NEES는 평가 지표이므로 허용 |
| 44. conditional adaptive-R ablation | 수신 신호에서 TOA/TDOA/DOA 추출 | GCC-SRP disagreement, block/total NIS | trajectory 생성, RMSE, NEES | 통과 |
| 46. large-scale routing validation | 44와 동일 | GCC-SRP disagreement, NIS | trajectory 생성, RMSE, NEES | 통과 |
| 61. static carrier-hop validation | matched filter TOA, GCC-PHAT TDOA, gated SRP-PHAT DOA | GCC-SRP disagreement, NIS | static position으로 signal synthesis와 RMSE 계산 | 통과 |
| 63. moving carrier-hop validation | 61과 동일 | GCC-SRP disagreement, NIS | signal synthesis, RMSE, offline elevation residual lag-1 diagnosis | 통과. 단 lag-1은 성능 입력이 아니라 기전 진단으로만 사용 |
| 82. quasi-static boundary | 61과 동일 | GCC-SRP disagreement, NIS | signal synthesis, RMSE, offline elevation residual lag-1 diagnosis | 통과. 속도 경계 claim은 0.005 m/s까지만 |
| 160. four-carrier independent validation | 61과 동일 | GCC-SRP disagreement, NIS | signal synthesis, settled error/P90/lag diagnosis | 통과. four-carrier는 실패 결과 |
| 161. sparse-tail mechanism diagnostic | 160 결과를 본 뒤 선택한 post-hoc geometry 재생 | 성능 claim 없음 | mechanism diagnosis | 통과. 일반화/성능 claim 금지 |
| 162. TOA transition guard pilot | 161 post-hoc geometry 재사용 | carrier change + observed TOA jump + disagreement/NIS | pilot 평가 | 통과. independent validation 전 claim 금지 |

## 9. 결과 JSON 표준 metadata

다음 실험부터 결과 JSON의 `config`에는 가능한 한 아래 키를 포함한다.

```json
{
  "stage": "development | validation | independent_validation | post_hoc_diagnostic | pilot",
  "protocol_frozen_before_execution": true,
  "automatic_manuscript_update_allowed": false,
  "distance_m": 600.0,
  "distances_m": [100, 200, 400, 600],
  "steps": 20,
  "settle_start": 10,
  "trajectory_count": 20,
  "motion_conditions": [],
  "seed_roots": {
    "geometry": 0,
    "ping": 0,
    "channel": 0,
    "bootstrap": 0
  },
  "common_random_ping_seeds": true,
  "array": {
    "sensor_count": 8,
    "ring_radius_m": 0.033,
    "vertical_spacing_m": 0.079
  },
  "signal": {
    "sample_rate_hz": 192000,
    "carrier_hz_or_schedule_hz": [],
    "chirp_bandwidth_hz": 12000,
    "pulse_duration_s": 0.010,
    "direct_path_gate_s": 0.005
  },
  "channel": {
    "sound_speed_m_s": 1500,
    "water_depth_m": 100,
    "receiver_depth_m": 30,
    "paths": ["direct", "surface", "bottom"],
    "snr_db_values": [10, 20, 30]
  },
  "truth_usage": "truth is not used for measurement extraction or adaptive decisions",
  "claim_allowed": "state exact allowed claim",
  "claim_forbidden": "state exact forbidden claim"
}
```

필수 metric:

- mean, median, P90 또는 tail metric
- divergence rate 또는 div50 rate
- paired gain
- bootstrap confidence interval
- paired test p-value
- improved fraction
- per-condition breakdown when conditions are heterogeneous

## 10. 이번 감사에서 즉시 처리한 보완

- `63. 이동표적 도약 대규모검증 백색화 확인/results/moving_validation.json`을 정상 JSON으로 복구했다.
- 63번 JSON config에 `settle_start`, `condition_details`, `fixed_carrier_khz`, `hop_carriers_khz`, `seed_roots`, `truth_usage`를 추가했다.
- 63번 핵심 수치는 변경하지 않았다: pooled moving mean gain `-0.10321251584048863 m`, CI `[-2.8137759189700717, 2.8012718560057697]`, Wilcoxon p `0.3009655989495688`, lag-1 fixed `0.4699501789122653` → hop `-0.20808210107765762`.
