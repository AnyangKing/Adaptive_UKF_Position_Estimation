# Adaptive UKF 위치추정 프로젝트 최신 인계

═══════════════════════════════════════════════════════════════════════════════
## ★★★ 프로젝트 산출물 형식 규약 (2026-07-12, 사용자 지시 — 최우선·불변)

- **이 프로젝트의 모든 문서 산출물은 LaTeX로 작성한다.** 논문 원고·표·수식·그림 캡션 등 최종물은
  `.tex`로 만든다. **Word/DOCX/`.docx`는 사용하지 않는다.**
- **원고 작성 언어 순서(2026-07-16 사용자 지시): 한글 기준 원고를 먼저 완성한 뒤 영어 원고로 옮긴다.**
  사용자가 직접 논리와 주장 범위를 읽고 판단할 수 있도록 `paper/manuscript_ko.tex`를 기준 원고로 두고,
  한글 원고에서 장 구조·claim boundary·수치 해석을 확정한 다음 `paper/manuscript.tex` 영어 IEEEtran 원고에
  반영한다. 기존 영어 원고는 보존하되, 앞으로의 원고 판단 기준은 한글 원고가 우선이다.
- **⚠ 2026-07-14 정정 이력: 병렬 세션(영문 132)이 IEEE JOE 문구를 "저널 미정"으로 오인해 중립화
  했고, 142에서 복원함. IEEE JOE 확정은 유효 — 다시 중립화하지 말 것.** 또한 **폴더 번호 131~139가
  한글/영문 두 시리즈로 충돌**(142 README 대응표). **새 폴더 만들기 전 반드시 `ls`로 최신 번호 확인**,
  142부터 단일 시리즈.
- **투고 저널 확정(2026-07-13): IEEE Journal of Oceanic Engineering (IEEE JOE).** IEEE JOE는 표준
  IEEEtran journal 클래스를 쓰므로 현재 `\documentclass[journal]{IEEEtran}`이 그대로 정답 —
  "저널 정해지면 양식 전환" 계획은 이로써 완료(추가 전환 불필요). 134번에서 IEEE JOE 관례로
  조정 완료: running head를 IEEE JOE로, Funding을 \thanks 각주로 이동, MDPI식 섹션(Author
  Contributions·Conflicts of Interest) 제거, IEEE식 Acknowledgment 유지. (※ "Ocean Engineering/
  elsarticle"[Elsevier]는 다른 프로젝트와 혼동한 착오로 철회됨 — IEEE JOE와 다름, 되돌리지 말 것.)
- **함의**: 100~107번의 Word 포맷/DOCX 변환 라인(formatting prep, DOCX dry run 등)은 이 지시로
  **대체(superseded)**된다. 앞으로 원고 마감은 `manuscript.tex`(IEEEtran, 범용) + `refs.bib` 방향.
  기존 DOCX 산출물은 커밋 이력이므로 삭제하지 않되, 더 발전시키지 않는다.
- **수식**: Markdown fenced math가 아니라 LaTeX 네이티브(`equation`/`align`)로. 이미 확정된 수식·표·
  수치·claim 경계는 그대로 옮기며, 내용은 바꾸지 않고 형식만 LaTeX로 이관한다.
- **LaTeX 파일 위치·편집 규칙 (2026-07-12 지정, 2026-07-13 위치 이동):** 원고 LaTeX 실체
  (`manuscript.tex`, `refs.bib`, `latexmkrc`, `figures/`)는 **`paper/` 폴더**에 둔다. (초기엔 루트에
  뒀으나 `.bbl`/`.aux`/`.log` 빌드 아티팩트가 운영 MD들과 섞여 지저분해져, 사용자 지시로 `paper/`로
  분리·이동함. 빌드 아티팩트도 전부 `paper/` 안에서 생겨 루트는 깨끗.) **numbered 폴더마다 복사하지
  않는다.** 폴더가 진행돼도 `.tex`는 `paper/`에 그대로 두고 **거기서 직접 수정(edit in place)**한다.
  numbered 폴더는 "무엇을·왜 바꿨는지" 기록·근거만 담는다.
- **GitHub 커밋/푸시 규약 (2026-07-13 사용자 재확인 — 최우선):** 사용자가 명시적으로 올리라고 한 것은
  **숫자 붙은 작업 폴더들만**이다. `paper/` 안의 논문 실체 파일(`manuscript.tex`, `refs.bib`,
  `latexmkrc`, `figures/`, `manuscript.pdf` 등), 루트 운영 MD, 공부 파일, 보고서 파일은 GitHub에
  올리지 않는다. 커밋할 때 `git add .` 금지. 반드시 해당 numbered folder 경로만 선택해서 stage/commit/push한다.
  과거 커밋에서 `paper/`가 이미 추적 중일 수 있으므로, 다음 AI는 `git status`에 `paper/` 변경이 보여도
  절대 stage하지 말고 사용자 지시 없이 `paper/` 추적해제도 임의 수행하지 않는다.
- 이 환경에 **MiKTeX 설치됨**(`pdflatex`/`latexmk`/`xelatex`) → `paper/`에서 직접 컴파일·PDF QA 가능.
  **원커맨드 빌드: `cd paper && latexmk manuscript.tex`**(`paper/latexmkrc`가 설정). 첫 실행 전
  `initexmf --set-config-value "[MPM]AutoInstall=1"` 권장(패키지 자동설치 무프롬프트). PDF 미리보기는
  `paper/`에서 `pdftoppm -png -r 90 manuscript.pdf page`(MiKTeX 번들). 빌드 상세=폴더 110·113.
- 다른 AI/세션은 이 규약을 먼저 읽고 따를 것. (동일 문구가 `연구_인계_현황.md`,
  `논문_초고_구조.md`에도 있음.)
═══════════════════════════════════════════════════════════════════════════════

작성/갱신: 2026-07-13  
상태: **113번까지 완료 — LaTeX/IEEEtran 원고를 `paper/`로 분리.** `paper/manuscript.tex`
(IEEEtran journal)+`paper/refs.bib`+`paper/figures/`(Fig.1~6)+`paper/latexmkrc`. 108~112는
IEEEtran 이관·QA 이력, 113은 paper 폴더 이동 단계다.
**원고 수정은 `paper/manuscript.tex`에서 직접(edit in place).**
**★ AI 완결 가능 작업의 경계:** 남은 것은 본질적으로 사용자 확정 사항 — 저자·소속·Funding·Data
Availability(원고에 placeholder), 저널 선택+그에 따른 template/length 조정, 최근접 선행연구 원문
대조 1회. 이건 지어낼 수 없으므로, 이것들이 정해지기 전엔 추가 자율 folder 생성 안 함(busywork 금지).
투고 체크리스트=`110.../submission_checklist_latex.md`. (이하 107까지 이력은 하단.)

(과거) **107번까지 완료(GitHub 동기화 확인 2026-07-12: 로컬=원격, 미완결 커밋 없음).**
92~107 논문 조립·마감 라인 진행: v2 통합(92) → Method 코드대조(93) → **v3 패치반영(94)** →
Fig.5 PNG·그림패키지(95) → **인용 placeholder 실서지 마감(96, v4-refs)** → 교수리뷰 패키지(97) →
교수 출장 대응 자율진행 전략(98) → 저널중립 체크리스트(99) → 포맷 전처리·Fig.1 폴리싱·그림 manifest
(100~102) → Sensors 형식 dry run·제출패키지·초록압축(103~105) → DOCX 변환계획·dry run(106~107).
**단 106~107 DOCX 라인은 위 LaTeX 규약으로 대체됨** — 다음은 원고를 IEEEtran `.tex`로 이관.
원고 최신 소스: `105/manuscript_sensors_candidate_tightened.md`(수치·claim 경계 무결 확인). 렌더 QA는
LibreOffice 부재로 미완(환경 한계) — LaTeX 전환으로 불필요해짐.

(과거 상태 기록) **91번까지 완료.** 실험 탐색은 82번(준정지 속도 경계 검증: 전체 +1.49m p=8e-5, whitening 재확인
p=9.2e-8; 단 속도별 비단조라 연속 quasi-static claim은 **0.005 m/s까지만 안전** — 본체 claim은 static
long-range)으로 종결. 논문 조립은 83~91: Abstract 초안(83) → 원고 skeleton(84) → **Manuscript v0(85)
→ v1(89)** → Citation audit 프로토콜(86) → Method 수식 정리(87) → v1 통합블록(88) → Tables draft(90)
→ Figure file alignment(91)까지 완료. **다음 폴더 = manuscript v2**(90 tables + 91 figures 통합;
Fig.7은 별도 생성 없으면 Table 1로 대체 — 91 결정). 절 초안·표·그림이 모두 폴더별로 존재하므로 v2는
통합·정합 작업이다. STUDY 미러는 01~25 전부 정합(2026-07-09 확인).

## 1. 지금 연구의 한 문장

처음 출발점은 “8센서 USBL 배열에서 TOA, TDOA, DOA를 추출해 UKF/Kalman 계열로 3D 위치를 추정하자”였다. 연구가 진행되며 진짜 병목은 필터 자체가 아니라 얕은바다 coherent multipath가 만드는 DOA 고도각 편향이라는 점이 드러났고, 최종적으로는 정지/준정지 표적에서 송신 반송파를 ping마다 도약시키는 frequency-agile pinging으로 그 편향을 백색화/상쇄하는 방향이 가장 강한 논문축으로 남았다.

## 2. 변하지 않는 중심축

- 센서 배열: 8센서 소형 USBL 배열
- 관측: TOA 1개, TDOA 7개, DOA 2개
- 상태: `[x, y, z, vx, vy, vz]`
- 추정기: UKF 중심. EKF/NLS는 비교 대상으로 사용
- 환경: 직접파, 표면/해저 반사, 유색잡음, 음속/동기 오차 등 현실적 수중 조건 고려

## 3. 연구가 뻗어나간 흐름

### 3.1 기본 TOA/TDOA/DOA-UKF

초기에는 TOA/TDOA/DOA를 한 관측벡터로 만들고 UKF/EKF/NLS로 위치를 추정했다. 비교 결과 EKF는 장거리 선형화 붕괴가 크고, NLS는 단일 snapshot 성능은 좋지만 동역학/일관성이 부족했다. UKF가 가장 안정적인 기본 추정기로 남았다.

### 3.2 조건부 adaptive-R 라우팅

GCC와 SRP 기반 DOA가 서로 불일치할 때 관측 공분산 R을 조건부로 조절하는 adaptive-R routing UKF를 만들었다. plain UKF 대비 전체 RMSE 약 17% 개선, 발산 감소, NEES/NIS 일관성 개선이 있었다. 그러나 장거리 600 m에서는 여전히 큰 오차가 남았다.

### 3.3 장거리 병목 규명: DOA 고도각 편향 floor

37~57번에서 여러 방향을 시도했다.

- DOA bias 상태 추가
- SRP peak margin 보정
- 거리별 bias 보정
- R 팽창
- 경로 가설 주변화
- 반사경로 TDOA 활용
- 캘리브레이션 lookup
- 현실채널/굴절 feature
- subgrid DOA 보간
- ping 수 증가

대부분 실패하거나 제한적이었다. 결론은 명확했다.

> 장거리 오차의 핵심은 랜덤 잡음이 아니라 소형 USBL 배열과 coherent multipath가 만드는 결정론적 DOA 고도각 편향이다.

주요 근거:

- 배열 회전대칭에 고정된 편향
- blind feature로 예측 어려움
- 캘리브레이션 lookup 일반화 실패
- 600 m CRLB 초과 bias floor 약 3.45 m
- grid resolution 문제가 아니라 편향 문제

### 3.4 frequency-agile pinging 발견

58번에서 새 레버를 발견했다. 고정 반송파에서는 직접파와 표면반사 사이의 상대 위상

```text
phi = 2π f δ
```

가 고정되어 DOA 편향도 시간상관적으로 고정된다. 반송파 `f`를 ping마다 30~34 kHz 범위에서 조금씩 바꾸면 간섭 위상이 회전하고, 편향이 ping 간 백색화된다.

중요한 점은 “어느 한 주파수를 고르는 것”이 아니라 “한 주파수에 머물지 않는 것”이다.

### 3.5 정지/준정지 표적에서 성공

60~61번에서 정지 표적 검증을 수행했다.

- 600 m 정지 표적
- fixed 32 kHz RMSE 약 13.01 m
- frequency hop RMSE 약 8.87 m
- 약 32% 개선
- 평균 이득 +4.14 m
- p = 0.0008
- median 기준 약 43% 개선

이것이 현재 논문 본체의 가장 강한 결과다.

### 3.6 이동 표적 확장과 한계

63번에서 이동 표적에서도 hop은 고도각 오차 lag-1 자기상관을 크게 낮췄다.

- fixed lag-1: +0.470
- hop lag-1: -0.208
- p = 5.56e-10

하지만 pooled RMSE 이득은 유의하지 않았다.

- 이동 표적 pooled 이득: -0.10 m
- p = 0.301

해석: 이동 표적은 기하 `δ` 자체가 시간에 따라 변하므로, 고정 반송파에서도 편향이 어느 정도 자연 진동·평균된다. 이를 motion-induced self-whitening으로 본다.

64~67번에서는 이동표적 안전화를 위해 adaptive-R, jump gate, NIS guard, anchor-hop schedule, condition-aware schedule을 시도했으나 모두 보편 개선법으로는 미재현됐다. 이동표적 schedule은 논문 본체가 아니라 한계 분석/future work로 둔다.

## 4. 최종 논문 포지셔닝

레이더 분야에는 pulse-to-pulse frequency agility로 glint/angle error를 탈상관하는 고전 문헌이 있다. 따라서 “우리가 frequency agility로 각도오차 백색화를 최초 발명했다”고 주장하면 위험하다.

정직한 기여는 다음이다.

> 레이더 glint 탈상관 원리를 얕은바다 USBL의 coherent multipath DOA bias 문제로 이식하고, 수중 고유의 게이트-내 표면반사 기전, 정지/이동 적용 경계, 그리고 정지표적 대규모 독립검증을 제시했다.

기여는 C1~C4로 정리한다.

- C1. gated-SRP USBL에서 게이트 안 표면반사 coherent leakage가 결정론적 고도각 편향을 만든다는 점 규명
- C2. 반송파 도약이 `phi = 2π f δ`를 회전시켜 편향을 백색화함을 입증
- C3. 정지/준정지 표적에서 600 m RMSE -32%, p=0.0008 대규모 독립검증
- C4. 이동 표적에서는 motion self-whitening으로 이득이 소멸하고, schedule 안전화도 미재현된다는 적용 경계 규명

## 5. 현재 최종 논문 구조

최신 구조는 `논문_초고_구조.md`의 `★★★ 최종 확정 구조` 블록을 우선한다.

권장 섹션:

1. Introduction
2. System & Signal Model
3. The Post-Gating Bias Floor
4. Mechanism: Carrier-Sensitive Coherent Interference
5. Frequency-Agile Whitening: Static Validation
6. Whitening Evidence & Moving-Target Boundary
7. Toward Moving Targets: Negative Results
8. Discussion
9. Conclusion & Future Work

## 6. 핵심 수치

| 항목 | 값 | 출처 |
|---|---|---|
| 정지 600 m RMSE fixed→hop | 13.01 → 8.87 m | 61 |
| 정지 600 m 개선 | -32%, p=0.0008 | 61 |
| 정지 600 m median | 13.97 → 7.96 m, -43% | 61 |
| lag-1 자기상관 fixed→hop | +0.470 → -0.208, p=5.56e-10 | 63 |
| 이동 표적 pooled RMSE 이득 | -0.10 m, p=0.301 | 63 |
| 600 m CRLB 초과 floor | 3.45 m | 45 |
| 편향의 반송파 진동 성분 | 78~92% | 58 |

## 7. 현재 최신 파일 상태

최신 운영 문서:

- `논문_초고_구조.md`
- `실험_레지스트리.md`
- `연구_인계_현황.md`
- `실패_한계_지도.md`
- `study_exports/14_frequency_hop_백색화와_anchor_schedule.md`
- `study_exports/15_motion_aware_frequency_schedule.md`
- `study_exports/16_radar_glint와_수중_USBL_포지셔닝.md`
- `study_exports/17_논문그림_결과를_주장으로_바꾸기.md`

81번까지 번호 폴더는 GitHub에 올라가거나 올라갈 대상이다. 68번은 레이더 glint 선행연구 포지셔닝, 69번은 논문 그림 원천 데이터 감사, 70번은 핵심 정량그림 4종 1차 생성, 71번은 그림 캡션과 Results 문단 조립, 72번은 Fig.1 시스템 개념도, 73번은 문헌 서지 리스크 감사, 74번은 Introduction 초안, 75번은 Mechanism 절 초안, 76번은 Static Validation 절 초안, 77번은 Moving Boundary 절 초안, 78번은 Related Work 최근접 선행연구 차별표, 79번은 Discussion 절 초안, 80번은 Conclusion/Future Work 절 초안, 81번은 준정지 속도 경계 검증 설계다. 운영 문서와 study 파일은 로컬 전용이며, 사용자가 따로 요청하지 않는 한 GitHub 커밋 대상이 아니다.

## 8. 다음 할 일

실험 추가보다 논문 조립이 우선이다.

1. 82번 준정지 속도 경계 검증 구현/실행
2. Abstract 초안 작성
3. 전체 원고 조립 파일 생성
4. DB 접근 가능 시: 레이더 glint/frequency agility 고전 문헌 exact citation 및 JASA 2007/IEEE 2022 원문 확인
5. 지도교수에게 포지셔닝과 타깃 저널 확인

## 9. 반드시 기억할 주의점

- 63번은 이동 표적 RMSE 이득이 아니라 백색화 메커니즘 확인 결과다.
- 65번 pilot의 schedule 이득은 66~67에서 재현되지 않았다.
- 이동표적 adaptive schedule은 future work로 두는 것이 안전하다.
- 논문 본체는 정지/준정지 frequency-agile whitening이다.
- 레이더 glint 문헌을 선제 인용해 “원리 최초 발명”처럼 보이지 않게 해야 한다.
---

## 최신 인계 추가: 82번 완료

82번 `준정지 속도 경계 검증 실행`을 완료했다. 총 132 paired trials를 실행했고, 결과 파일은 82번 폴더의 `results/` 아래에 있다.

핵심:

- 전체 평균은 hop이 개선: fixed 11.98 m → hop 10.49 m, p = 8.00e-05.
- whitening은 재확인: lag-1 fixed +0.220 → hop -0.100, p = 9.17e-08.
- 연속 quasi-static boundary는 0.005 m/s까지만 안전하다.
- 0.030과 0.100 m/s도 양성이지만, 0.010과 0.050 m/s가 깨져서 단조 경계로 주장하면 안 된다.
- 논문 본체는 static 중심, quasi-static은 very slow drift 제한 표현, higher-speed 결과는 비단조/기하 의존 Discussion으로 처리.

다음 우선순위:

1. 83번으로 Abstract 초안 작성 또는 전체 manuscript assembly 시작.
2. 82번을 반영해 Moving Boundary/Discussion 문단 보강.
3. 추가 실험을 한다면 radial tail guard 또는 motion/geometry-aware schedule이지만, 본 논문 본체보다는 future work가 안전하다.
> 최신 정정(2026-07-09): 82번 `준정지 속도 경계 검증 실행`까지 완료됐다. Git HEAD/origin/main 커밋은 `c8dc8ad`이며 커밋명은 `82. 준정지 속도 경계 검증 실행`이다. 다음 우선순위는 83번 Abstract 초안 또는 전체 원고 조립이다. 아래에 81번까지 완료라고 남은 상단 문장은 과거 요약이므로, 하단 “82번 완료” 섹션을 우선한다.
---

## 최신 인계 추가: 83번 Abstract 초안 완료

83번 `Abstract 초안` 폴더를 작성했다. 82번 결과 이후 논문 초록에서 claim을 어디까지 말할 수 있는지 잠그는 단계다.

핵심:

- Version A/B/C 초록 후보를 만들었다.
- 현재 추천은 Version C: 선행연구를 먼저 인정하고 novelty를 mechanism + validation + boundary로 정의하는 버전.
- 초록 본체는 `static long-range USBL` 중심.
- `quasi-static`은 0.005 m/s very slow drift까지만.
- moving target은 residual whitening만 강하게 말하고 RMSE gain은 reliable하지 않다고 쓴다.
- 금지 표현: first frequency-hopping USBL, moving-target improvement, quasi-static up to 0.100 m/s.

다음 우선순위:

1. 84번 전체 manuscript skeleton.
2. 83번 초록을 기준으로 Introduction/Discussion/Conclusion 정합화.
3. exact citation audit 지속.
---

## 최신 인계 추가: 84번 전체 원고 skeleton 완료

84번 `전체 원고 skeleton` 폴더를 작성했다. 83번 초록에서 잠근 claim을 원고 전체 구조로 펼친 설계도다.

핵심:

- 9개 절 원고 구조 확정:
  1. Introduction
  2. System and signal model
  3. Post-gating DOA bias floor
  4. Carrier-sensitive coherent interference mechanism
  5. Frequency-agile whitening method
  6. Static validation
  7. Whitening evidence and applicability boundary
  8. Discussion
  9. Conclusion
- Fig.1~7, Table 1~3의 역할과 근거 폴더를 mapping했다.
- reviewer risk-response 문장을 작성했다.
- 다음 단계는 85번 `manuscript_draft_v0` 또는 Introduction 최종 초안이다.
---

## 최신 인계 추가: 85번 Manuscript draft v0 완료

85번 `Manuscript draft v0` 폴더를 작성했다. 이제 연구 결과들이 처음으로 하나의 논문 원고 흐름으로 연결됐다.

산출물:

- `manuscript_draft_v0.md`: Abstract부터 Conclusion까지 전체 초안.
- `revision_tasks.md`: v1로 가기 위한 citation/figure/method/수치 점검 과제.
- `claim_consistency_check.md`: 초록-본문-결론 claim 일관성 점검.

다음 우선순위:

1. 86번 Introduction/Related Work citation-ready 정제.
2. 또는 figure reference를 manuscript v0에 삽입.
3. exact citation audit은 더 미루면 위험하다.
---

## 최신 인계 추가: 86번 Citation audit protocol 완료

86번 `Citation audit protocol` 폴더를 작성했다. 실제 citation 확정이 아니라, 원고 v0의 placeholder를 안전하게 치환하기 위한 검증표다.

산출물:

- `citation_audit_matrix.md`
- `search_query_plan.md`
- `placeholder_replacement_plan.md`
- `bibtex_stub.md`

다음:

- 87번에서 실제 DB 접근이 가능하면 exact citation audit을 수행한다.
- 접근이 안 되면 placeholder-safe 상태로 manuscript v1 method/figure integration을 진행한다.
---

## 최신 인계 추가: 87번 Method equations integration 완료

87번 `Method equations integration` 폴더를 작성했다.

산출물:

- `method_equations_v1.md`
- `notation_table.md`
- `manuscript_insertion_plan.md`
- `equation_claim_check.md`

역할:

- 85번 원고 v0에 들어갈 수식 블록 준비.
- 핵심 수식은 `z_k = [TOA, TDOA..., azimuth, elevation]`, UKF state, `phi = 2*pi*f*delta + theta_r`.
- moving target self-whitening을 `delta(t)`로 설명.

다음은 88번에서 원고 v1에 수식/figure reference를 실제 삽입하는 것이 자연스럽다.
---

## 최신 인계 추가: 88번 Manuscript v1 integration blocks 완료

88번 `Manuscript v1 integration blocks` 폴더를 작성했다.

산출물:

- `v1_section_blocks.md`
- `figure_callouts.md`
- `v1_assembly_checklist.md`

역할:

- 85번 원고 v0에 87번 수식과 84번 figure map을 실제로 삽입하기 위한 교체 블록.
- 다음 단계는 89번 `manuscript_draft_v1.md` 생성이 가장 자연스럽다.
---

## 최신 인계 추가: 89번 Manuscript draft v1 완료

89번 `Manuscript draft v1` 폴더를 작성했다.

산출물:

- `manuscript_draft_v1.md`
- `v1_quality_check.md`
- `next_to_v2.md`

상태:

- v1에는 수식과 figure/table callout이 들어갔다.
- citation placeholder는 아직 유지.
- 다음으로 가장 자연스러운 작업은 90번 `Tables draft`: Table 1 prior-art differentiation, Table 2 numerical summary 작성.
---

## 최신 인계 추가: 90번 Tables draft 완료

90번 `Tables draft` 폴더를 작성했다.

산출물:

- `table1_prior_art_differentiation.md`
- `table2_numerical_results_summary.md`
- `table3_limitations_future_work.md`
- `table_insertion_notes.md`

요지:

- Table 1: novelty 방어용. "주파수 도약 자체 최초"가 아니라 수중 USBL의 post-gating coherent multipath DOA bias whitening과 TOA/TDOA/DOA-UKF 경계 검증이 기여라고 정리.
- Table 2: static 600 m 13.01 m -> 8.87 m, moving lag-1 whitening +0.470 -> -0.208 but pooled RMSE gain 없음, quasi-static continuous boundary 0.005 m/s, 0.030/0.100 m/s는 non-continuous geometry-dependent positive로 정리.
- Table 3: citation audit, real-water validation, moving schedule, carrier ablation, compact aperture floor를 future work/limitation으로 정리.

다음 추천:

1. 91번 `Figure file alignment`: Fig.1~6 실제 파일과 manuscript callout 번호를 맞춘다.
2. 또는 91번 `Manuscript v2 table integration`: 89번 원고 v1에 90번 표를 삽입해 v2로 만든다.
---

## 최신 인계 추가: 91번 Figure file alignment 완료

91번 `Figure file alignment` 폴더를 작성했다.

산출물:

- `figures/` canonical figure set
- `figure_manifest.md`
- `manuscript_figure_callout_fix.md`
- `figure_caption_drafts.md`

중요 결정:

- 원고 기준 figure numbering은 Fig.1~6으로 정리.
- 기존 70번 source의 `fig7_crlb_floor.*`는 원고에서는 Fig.6이므로 91번에서는 `fig6_crlb_floor.*`로 복사했다.
- Related-work positioning은 Fig.7을 새로 만들지 않고 90번 Table 1로 처리하는 편이 현재 원고 구조에 맞다.
- Fig.5 quasi-static speed boundary는 현재 SVG만 있다. Word 조립 전에 PNG 변환 필요 가능.

다음 추천:

1. 92번 `Manuscript v2 table figure integration`: 89번 v1에 90번 표와 91번 figure manifest/captions를 실제로 삽입.
2. 또는 Fig.5 PNG 변환을 먼저 수행.
---

## 최신 인계 추가: 94번 Manuscript v3 완료

94번 `Manuscript v3` 폴더를 작성했다.

산출물:

- `manuscript_draft_v3.md`
- `v3_change_log.md`
- `v3_quality_check.md`
- `next_submission_tasks.md`

무엇을 했나:

- 93번에서 발견한 §2 Method 불일치를 실제 원고에 반영했다.
- v2의 단순 adaptive-R 이진 수식을 실제 코드와 맞는 2단계 규칙으로 바꿨다.
  - GCC/SRP DOA disagreement scale: `s=min(100,1+(g/2)^2)`.
  - 5 deg threshold에 따른 DOA/TDOA block routing.
  - block-wise NIS inflation with chi-square thresholds 6.63/18.48/9.21.
- Section 2.1 implementation parameters를 추가했다.
- 5 ms direct-path gate 수치를 mechanism 설명에 넣었다.
- static/moving/quasi-static validation protocol 세부를 추가했다.

검증:

- `python "93. Method 세부 코드 대조/test_diagnostic.py"` PASS.
- v3 품질 체크에서 Draft v2 흔적, 이진 adaptive-R 수식, 미래형 callout 없음.
- `fig7`은 active figure callout이 아니라 legacy filename 설명에만 남음.

현재 남은 마감 작업:

1. citation placeholder 실서지 치환.
2. Fig.5 PNG 및 그림 마감.
3. 저널 포맷/수식번호/참고문헌 스타일.
4. 최종 영어 compression.

다음 추천 폴더:

- `95. Citation placeholder closure` if library/publisher access is available.
- Otherwise `95. Fig5 PNG and submission packaging`.
---

## 최신 인계 추가: 95번 Fig5 PNG and submission packaging 완료

95번 `Fig5 PNG and submission packaging` 폴더를 작성했다.

산출물:

- `generate_fig5_png.py`
- `figures/` 안에 Fig.1~6 PNG/SVG 전체 세트
- `figure_package_manifest.md`
- `submission_readiness_check.md`

핵심:

- 기존 Fig.5는 SVG만 있었는데, 82번 결과 JSON에서 PNG/SVG를 재생성했다.
- matplotlib GUI 문제를 피하기 위해 Agg backend를 사용했다.
- Fig.5는 x축을 categorical spacing으로 표시했다. 이유는 0.000/0.005/0.010 tick label이 실제 간격에서는 겹쳤기 때문이다.
- 해석은 바꾸지 않았다:
  - continuous validated region = 0.005 m/s까지.
  - 0.030/0.100 m/s positive는 non-continuous geometry-dependent recovery.
- 현재 figure package는 PNG 6개, SVG 6개로 교수님 리뷰/Word 조립에 바로 쓸 수 있다.

현재 남은 큰 blocker:

1. citation placeholder 실서지 치환.
2. 저널 포맷/수식번호/참고문헌 스타일.
3. 교수님 리뷰 반영.

다음 추천:

- 도서관/퍼블리셔 접근 가능하면 `96. Citation placeholder closure`.
- 아니면 `96. Professor review package`.
---

## 최신 인계 추가: 96번 Citation placeholder closure 완료

96번 `Citation placeholder closure` 폴더를 작성했다.

무엇을 했나:

- Crossref DOI metadata 기준으로 주요 citation placeholder 4개를 실제 서지로 닫았다.
- BibTeX 초안과 원고 치환표를 만들었다.
- 94번 v3를 기반으로 citation key가 들어간 `manuscript_draft_v4_refs.md`를 생성했다.
- 네 placeholder 잔존 count = 0 확인.

확인된 서지:

- Radar frequency agility/glint: Loomis & Graf, IEEE TAES, 1974, doi: 10.1109/TAES.1974.307889.
- Frequency-hopped USBL: Beaujean, Mohamed & Warin, JASA, 2007, doi: 10.1121/1.2400616.
- Costas USBL: Nhat et al., IMCOM, 2022, doi: 10.1109/IMCOM53663.2022.9721736.
- Frequency-comb iUSBL: Qian et al., IEEE IoT Journal, 2025, doi: 10.1109/JIOT.2025.3564346.

주의:

- 96번은 metadata closure다. full-text semantic audit은 투고 전 도서관/IEEE/publisher PDF로 한 번 더 하는 것이 안전하다.
- novelty 문장은 계속 보수적으로 유지해야 한다: frequency hopping 자체 최초가 아니라, 수중 USBL post-gating coherent DOA-bias whitening과 static/moving boundary가 기여다.

다음 추천:

- `97. Professor review package`: v4-ref 원고 + Fig.1~6 package + 핵심 주장/남은 질문을 교수님 보고용으로 묶기.
- 또는 `97. Journal formatting prep`: target journal template 맞춤 준비.
---

## 최신 인계 추가: 97번 Professor review package 완료

97번 `Professor review package` 폴더를 작성했다.

산출물:

- `professor_review_brief.md`
- `decision_questions.md`
- `current_submission_state.md`
- `review_package_manifest.md`

교수님께 보여줄 순서:

1. `97. Professor review package/professor_review_brief.md`
2. `97. Professor review package/decision_questions.md`
3. `96. Citation placeholder closure/manuscript_draft_v4_refs.md`
4. `95. Fig5 PNG and submission packaging/figures/`

핵심 보고 메시지:

- 출발점은 TOA/TDOA/DOA를 UKF로 융합하는 위치추정.
- 연구 중 장거리 오차의 병목이 필터 자체보다 post-gating coherent multipath DOA bias임을 확인.
- carrier-agile pinging으로 이 bias를 whitening해 static 600 m에서 13.01 m → 8.87 m, p=0.0008.
- moving target은 whitening은 되지만 RMSE 개선은 검증되지 않음.
- quasi-static continuous boundary는 0.005 m/s까지만.
- novelty는 frequency hopping 자체가 아니라 수중 USBL의 coherent DOA-bias whitening mechanism과 적용 경계 검증.

다음은 사용자/교수님 판단이 필요한 영역:

- target journal.
- simulation 중심으로 투고할지, real-water 검증 후 투고할지.
- full-text citation audit과 journal formatting 우선순위.
---

## 최신 인계 추가: 98번 Self-directed submission strategy 완료

98번 `Self-directed submission strategy` 폴더를 작성했다.

배경:

- 교수님이 출장 중이라 사용자가 직접 진행해야 하는 상황.
- 그래서 97번의 교수님께 물어볼 질문들을 "임시 기본 결정"으로 바꿨다.

산출물:

- `default_decisions.md`
- `journal_shortlist.md`
- `30_day_execution_plan.md`
- `risk_register_without_professor.md`
- `go_no_go_gates.md`

핵심 기본값:

- 논문 축은 static/quasi-static long-range USBL coherent DOA-bias whitening.
- moving target은 RMSE 개선이 아니라 mechanism whitening + applicability boundary.
- quasi-static continuous boundary는 0.005 m/s.
- 우선은 Sensors식 reproducible sensor-fusion / multi-sensor positioning 논문으로 준비.
- Applied Acoustics는 acoustic fit은 좋지만 experimental validation 요구가 있어 field/numerical validation framing 주의.
- IEEE/OES-style target은 더 야심찬 방향이며 real-water 검증 후가 더 안전.

다음 추천:

- `99. Journal neutral manuscript checklist`: target journal이 확정되지 않아도 원고 변환 준비를 진행할 수 있는 단계.
---

## 최신 인계 추가: 99번 Journal neutral manuscript checklist 완료

99번 `Journal neutral manuscript checklist` 폴더를 작성했다.

산출물:

- `manuscript_neutral_checklist.md`
- `section_conversion_map.md`
- `markdown_to_journal_tasks.md`
- `evidence_status_table.md`
- `next_folder_recommendation.md`

핵심 판정:

- 현재 최선 원고는 `96. Citation placeholder closure/manuscript_draft_v4_refs.md`.
- 주요 citation placeholder 4개는 잔존 0.
- Fig.1~6은 PNG/SVG 모두 있음.
- 미래형 callout과 오래된 Fig.5 PNG 미생성 문구는 없음.
- 과학적 원고 조립은 된 상태.
- 남은 것은 저널 포맷/수식번호/표 폭/참고문헌 스타일/최종 압축.

다음 추천:

- `100. Manuscript formatting prep`

---

## 최신 인계 추가: 100번 Manuscript formatting prep 완료

100번 `Manuscript formatting prep` 폴더를 작성했다.

핵심:

- 96번 `manuscript_draft_v4_refs.md`를 clean source로 가져와 투고 전처리용 `manuscript_clean_source.md`를 만들었다.
- 96번에서 citation metadata가 이미 닫혔으므로, 원고 안의 “metadata/citation pending” 뉘앙스 문구를 “final journal style pending”으로 최신화했다.
- 수식 23개 번호 계획, Fig.1--6/Table 1--3 삽입 계획, cover letter bullets, formatting todo, changelog, precommit verification을 만들었다.
- GitHub에는 이 100번 폴더만 올려야 한다. 로컬 인계/공부/교수님 보고 파일은 stage하지 않는다.

현재 원고 상태:

- 과학적 구조는 조립 완료.
- target journal style 변환은 아직 전.
- main positive claim은 static 600 m 13.01 m → 8.87 m, p=0.0008.
- moving target은 whitening evidence + boundary로만 다룬다.
- quasi-static continuous boundary는 0.005 m/s.
- sub-meter 600 m 주장은 하지 않는다.

다음 추천:

- `101. Target template conversion dry run` 또는 `101. Fig1 visual polish`.

---

## 최신 인계 추가: 101번 Fig1 visual polish 완료

101번 `Fig1 visual polish` 폴더를 작성했다.

핵심:

- 기존 Fig.1 concept draft를 대체할 polished schematic을 새로 만들었다.
- 출력 파일:
  - `101. Fig1 visual polish/figures/fig1_system_concept_polished.png`
  - `101. Fig1 visual polish/figures/fig1_system_concept_polished.svg`
- 재현 스크립트:
  - `101. Fig1 visual polish/make_fig1_polished.py`
- 새 caption:
  - `101. Fig1 visual polish/caption_update.md`

그림 메시지:

- Panel (a): direct path와 surface-reflected path가 5 ms DOA gate 안에서 coherent하게 처리될 수 있음을 보여준다.
- Panel (b): fixed 32 kHz는 phase locking/correlated DOA bias, 30--34 kHz agile pinging은 phase rotation/whitened residual로 표현한다.
- 같은 TOA/TDOA/DOA adaptive-R UKF로 들어가므로 estimator를 바꾼 것이 아니라 observation residual 구조를 바꾼다는 메시지를 유지한다.

다음 추천:

- `102. Manuscript figure manifest update`: 100번 clean source의 Fig.1 caption/manifest를 101번 그림으로 갱신.
- 또는 `102. Target template conversion dry run`.

---

## 최신 인계 추가: 102번 Manuscript figure manifest update 완료

102번 `Manuscript figure manifest update` 폴더를 작성했다.

핵심:

- 100번 clean source를 복사해 `manuscript_clean_source_fig1_updated.md`를 만들었다.
- Fig.1 caption을 101번 polished figure용 caption으로 교체했다.
- Figure File Manifest의 Fig.1 경로를 `101. Fig1 visual polish/figures/fig1_system_concept_polished.svg` / `.png`로 바꿨다.
- Fig.2--6, 성능 수치, references, equation, claim boundary는 변경하지 않았다.

현재 manuscript source 우선순위:

1. `102. Manuscript figure manifest update/manuscript_clean_source_fig1_updated.md`
2. figure source: Fig.1은 101번, Fig.2--6은 95번 package

다음 추천:

- `103. Target template conversion dry run`
- 또는 `103. Submission package assembly dry run`

---

## 최신 인계 추가: 103번 Sensors free-format dry run 완료

103번 `Sensors free-format dry run` 폴더를 작성했다.

핵심:

- 2026-07-12 기준 Sensors 공식 author instructions를 확인해 free-format submission 요구사항을 정리했다.
- 102번 최신 원고 source를 `manuscript_sensors_free_format_candidate.md`로 복사했다.
- Keywords와 back matter placeholders를 추가했다:
  - Supplementary Materials
  - Author Contributions
  - Funding
  - Data Availability Statement
  - Acknowledgments
  - Conflicts of Interest
- Fig.1은 101번 polished figure, Fig.2--6은 95번 package를 사용하도록 asset map을 정리했다.
- 초록은 약 205 words라 Sensors target이면 다음 polishing에서 5--20 words 줄이면 좋다.

현재 blocker:

- 인간 저자 정보/기여도/funding/conflict/data availability final statement.
- Markdown table → native table 변환.
- fenced math → editable equation 변환.
- final figure package assembly.

다음 추천:

- `104. Final submission asset package dry run`
- 또는 `104. Abstract and back-matter tightening`

---

## 최신 인계 추가: 104번 Final submission asset package dry run 완료

104번 `Final submission asset package dry run` 폴더를 작성했다.

핵심:

- `manuscript/`에 103번 원고 후보를 복사했다.
- `figures/`에 Fig.1--6 PNG/SVG를 모두 모았다.
  - Fig.1: 101번 polished figure
  - Fig.2--6: 95번 figure package
- `supplement_candidates/`에 source map과 data/code availability plan을 만들었다.
- `checks/`에 package completeness check와 claim boundary check를 만들었다.

검증:

- manuscript 1개.
- PNG 6개.
- SVG 6개.
- scientific claim boundary 유지.
- 이 패키지는 실제 제출용이 아니라 제출 전 결손 확인용이다.

아직 필요한 것:

- author/funding/conflict/contribution/data availability 최종 문구.
- editable equation 변환.
- native table 변환.
- final reference style.
- final Word/LaTeX template.

다음 추천:

- `105. Abstract and back-matter tightening`
- 또는 `105. Editable equation and table conversion plan`

---

## 최신 인계 추가: 105번 Abstract and back-matter tightening 완료

105번 `Abstract and back-matter tightening` 폴더를 작성했다.

핵심:

- 104번 원고 후보를 `manuscript_sensors_candidate_tightened.md`로 복사.
- 초록을 205 words → 198 words로 축약.
- static 600m 13.01→8.87m, p=0.0008 유지.
- moving-target RMSE 개선 미주장 유지.
- quasi-static continuous boundary 0.005m/s 유지.
- novelty = frequency hopping 자체가 아니라 mechanism/validation/boundary라는 표현 유지.
- Author Contributions/Funding/Data Availability/Acknowledgments/Conflicts는 human-author decision required로 명확히 표시.

현재 최신 원고 후보:

- `105. Abstract and back-matter tightening/manuscript_sensors_candidate_tightened.md`

다음 추천:

- `106. Editable equation and table conversion plan`

---

## 최신 인계 추가: 106번 Editable equation and table conversion plan 완료

106번 `Editable equation and table conversion plan` 폴더를 작성했다.

핵심:

- 105번 최신 원고 기준:
  - fenced math block 23개
  - Markdown table line 37개
- 수식 변환 그룹:
  - geometry/observation model
  - UKF/adaptive-R
  - carrier-sensitive phase mechanism
  - fixed/agile frequency policy
  - RMSE/lag-1 validation metrics
- 표 변환 결정:
  - Table 1: novelty defense, main text 유지 우선
  - Table 2: claim boundary, negative moving result와 0.005 m/s boundary 보존
  - Table 3: page limit에 따라 prose/supplement 가능
  - Figure File Manifest: final article body에서 제거

다음 추천:

- `107. Editable manuscript conversion dry run`

---

## 최신 인계 추가: 107번 Editable manuscript conversion dry run 완료

107번 `Editable manuscript conversion dry run` 폴더를 작성했다.

핵심:

- 105번 최신 원고를 DOCX dry run으로 변환.
- documents 스킬 사용.
- preset: `compact_reference_guide`.
- 산출물:
  - `editable_manuscript_conversion_dry_run.docx`
  - `scripts/build_docx_dry_run.py`
  - `conversion_dry_run_report.md`

구조 검증:

- paragraphs 130
- native Word tables 4
- images 6
- editable equation paragraphs 23

주의:

- render_docx.py 렌더 QA는 시도했지만 LibreOffice/soffice 실행 파일을 찾지 못해 실패했다.
- 따라서 DOCX는 구조 변환 dry run일 뿐, 최종 시각 QA 통과본은 아니다.

다음 추천:

- `108. Word conversion structural refinement`
- 또는 LibreOffice/soffice 가능한 환경에서 107번 DOCX render QA 재시도
## 2026-07-16 중단 지점: 162번 완료 후 정지

최신 과학 결과는 162번 `Carrier transition TOA guard pilot`이다. 160번에서 sparse four-carrier가
신규 20기하 중 한 곳에서 53.001 m로 발산했고, 161번은 그 원인이 3.557 m carrier-dependent
TOA branch를 cycle이 9회 반복 횡단한 것임을 보였다. 162번은 carrier change+raw range jump
>0.5 m일 때 absolute TOA만 격리하고 TDOA/DOA는 유지하는 guard를 구현했다. 같은 post-hoc
3기하에서 catastrophic RMSE 53.001→8.224 m, four 평균 23.198→8.273 m, linear/fixed 보존.

아직 독립 성능 근거가 아니다. guard 독립검증은 후속 후보로 남기되, 2026-07-16 사용자 지시로
다음 우선순위가 바뀌었다. 이제는 영어 원고 직행이 아니라 `paper/manuscript_ko.tex` 한글 기준
원고를 먼저 완성하고, 사용자가 읽어 구조·claim·수치 해석을 확정한 뒤 영어 IEEEtran 원고에 반영한다.

## 2026-07-16 최신 인계: 163번 Korean-first manuscript workflow

163번은 연구 실험이 아니라 원고 작성 방식 전환이다. `paper/manuscript.tex` 영어 원고는 보존했고,
새로 `paper/manuscript_ko.tex` 한글 기준 원고 v0를 만들었다. 한글 원고에는 출발점
TOA/TDOA/DOA-UKF, 장거리 post-gating coherent multipath DOA bias floor, carrier-agile whitening,
정지 600 m 13.01→8.87 m(p=0.0008), moving whitening-only boundary, quasi-static 0.005 m/s 경계,
160~162 schedule/guard 결과의 claim 제한을 반영했다.

다음 우선순위는 164번 `Korean manuscript structure pass`다. 한글 원고의 장 번호, 표/그림 callout,
Discussion/future work 위치를 다듬고, 그 후 영어 `paper/manuscript.tex`에 반영한다. guard 신규 seed
검증은 논문 본체보다 후순위다.

## 2026-07-20 최신 인계: 164번 Korean manuscript structure pass

164번은 한글 기준 원고 구조 패스다. `paper/manuscript_ko.tex`를 v1로 갱신했고, 핵심어, 한글 기준
원고 역할 설명, claim boundary 표, 검증 요약 표, 실패/한계 배치 표를 추가했다. Fig.1 system,
Fig.6 CRLB/floor, two-ray fit, carrier bias, static 600 m, moving whitening, quasi-static boundary 그림
callout도 본문에 배치했다.

중요 결정: 160~162번 schedule/guard 결과는 흥미롭지만 독립검증 전이므로 본문 성능 claim이 아니다.
한글 원고에서는 limitation/future work로 고정했다. 다음 우선순위는 165번 `Korean manuscript language
tightening`: 사용자가 읽기 편하도록 중복 문장을 줄이고, 수식·표·그림 주변 설명을 더 자연스럽게 다듬는 단계다.

## 2026-07-20 최신 인계: 165번 Korean manuscript language tightening

165번은 한글 기준 원고 문장 다듬기다. `paper/manuscript_ko.tex`를 v2로 갱신했고, 초록·한글 원고
역할 설명·서론·기여 문단의 반복 표현을 줄였다. 핵심 claim과 수치는 바꾸지 않았다. 유지된 경계는
정지 600 m 13.01→8.87 m(p=0.0008), moving residual whitening only, pooled RMSE gain 미재현,
quasi-static 0.005 m/s, 160~162는 limitation/future work다.

다음 우선순위는 166번 `Korean manuscript claim audit`이다. 한글 원고 v2의 모든 핵심 수치와 claim
boundary를 기존 실험 폴더/레지스트리와 대조한다.

## 2026-07-20 최신 인계: 166번 Korean manuscript claim audit

166번은 한글 기준 원고 claim 감사다. `paper/manuscript_ko.tex` v2의 핵심 수치를 61/63/82/93/160/161/162번
폴더 및 `실험_레지스트리.md`와 대조했다. 수치 불일치 0건. 정지 600 m 13.01→8.87 m, improvement
4.14 m, p=0.0008; 이동 lag-1 +0.470→-0.208 및 pooled RMSE gain 미재현; 준정지 11.98→10.49 m와
0.005 m/s 안전선; 160~162 limitation 배치가 모두 정합했다.

보정 1건: 초록과 claim 표의 `정지/준정지` 표현이 82번의 0.005 m/s 제한보다 넓게 읽힐 수 있어
`정지 및 매우 느린 준정지`로 좁혔다. 다음 우선순위는 167번 `Korean figure table caption pass`다.
그림·표 캡션이 무엇을 주장하고 무엇을 주장하지 않는지 더 명확히 다듬는다.

Git 규칙: numbered folder만 exact-path stage, `git add .` 금지. paper/, 루트 MD, study_exports는
절대 stage/push하지 않는다. 폴더명과 commit message를 정확히 같게 한다.

## 2026-07-27 최신 인계: 168번 Korean abstract and introduction tightening

168번은 한글 기준 원고 안정화의 다음 단계다. `paper/manuscript_ko.tex`를 v3로 갱신했고, 초록과
서론이 166번 claim audit 및 167번 caption policy를 넘지 않도록 다듬었다.

핵심 변경:

- 초록에서 병목을 `필터 내부`가 아니라 `direct-path gate 안 coherent DOA bias`로 더 선명히 표현.
- carrier-agile pinging을 `새 localization solver`가 아니라 `송신 측 관측 설계`로 고정.
- 서론 첫 문단의 반복을 줄이고 TOA/TDOA/DOA-UKF 출발점을 압축.
- 기여 문단의 `준정지`를 `매우 느린 준정지`로 좁힘.

유지한 경계:

- 정지 600 m 13.01→8.87 m, p=0.0008
- moving pooled RMSE gain 미주장
- quasi-static 0.005 m/s 안전선
- frequency hopping USBL 최초성 금지
- 160~162번 schedule/guard 결과는 본문 성능 claim 금지

다음 우선순위는 169번 `Korean discussion and conclusion tightening`이다. Discussion/Conclusion에서
한계, future work, 실해역 검증 필요성을 정직하게 배치한다.

## 2026-07-27 최신 인계: 169번 Korean discussion and conclusion tightening

169번은 한글 기준 원고의 Discussion/Conclusion 안정화다. `paper/manuscript_ko.tex`에서
carrier-agile pinging의 대상을 `bias floor 전체`가 아니라 `carrier-locked 성분`으로 좁히고,
방법 효과를 `관측 잔차의 시간상관을 낮춘다`는 표현으로 정리했다.

중요 변경:

- 160~162번 결과는 방법 확장이 아니라 schedule 설계 한계와 future work 근거로만 사용한다고 명시.
- 실해역/수조 검증은 남은 과제이며, 투고 전 보강 또는 future work 중 교수님과 결정할 항목으로 둠.
- Conclusion에서 장거리 floor 전체를 단일 원인으로 등치하지 않고 `post-gating coherent multipath DOA bias와 연결됨`으로 완화.
- 결론 마지막 문장을 `coherent multipath bias floor를 줄인다`가 아니라 `carrier-locked coherent multipath DOA bias의 시간상관을 낮춘다`로 좁힘.

다음 우선순위는 170번 `Korean manuscript v3 integration audit`이다. 초록·서론·본문·Discussion·Conclusion·표·그림 캡션이 모두 같은 claim boundary 안에 있는지 최종 대조한다.
## 실험 감사 운영 규칙 (2026-08-11 추가)

루트 문서 `실험_감사_체크리스트.md`를 새로 만들었다. 이 문서는 numbered 연구 폴더가 아니라 프로젝트 전체 품질관리 문서로 유지한다. 앞으로 새 실험을 시작할 때는 README 또는 protocol에 이 체크리스트 기준을 반영하고, 실험 종료 후 claim boundary를 다시 확인한다.

이번 감사에서 확정한 운영 원칙:
- ground truth는 신호 합성, 최종 오차 계산, offline mechanism diagnosis에만 사용한다.
- TOA/TDOA/DOA 추출, 관측 신뢰도, adaptive-R, carrier schedule/guard 결정에는 truth를 쓰지 않는다.
- 채택 관측은 matched-filter TOA, GCC-PHAT TDOA, 5 ms gated SRP-PHAT array-level DOA로 제한한다.
- 현재 canonical USBL protocol은 one-way synchronized beacon 가정이다. common clock bias, sensor별 hardware delay, sensor mismatch는 canonical validation에 포함되지 않았으므로 practical USBL 성능으로 과장하지 않는다.
- pilot/post-hoc/oracle 결과는 성능 claim에 쓰지 않는다.
- 결과 JSON에는 seed roots, schedule, settled window, truth_usage, allowed/forbidden claim을 가능한 한 저장한다.

즉시 보완 완료:
- `63. 이동표적 도약 대규모검증 백색화 확인/results/moving_validation.json`의 깨진 JSON을 정상 복구했다.
- 63번 JSON config에 `settle_start`, `condition_details`, `fixed_carrier_khz`, `hop_carriers_khz`, `seed_roots`, `truth_usage`를 추가했다.
- 수치는 변경하지 않았다. moving pooled gain은 여전히 `-0.103 m`, CI `[-2.814, +2.801]`, p `0.301`; lag-1은 fixed `+0.470`에서 hop `-0.208`.

## 공부 문서 생성 규칙 (2026-08-10 추가)

앞으로 `study_exports/` 및 `C:\Users\HOSEO\OneDrive - 호서대학교\나는 개인이요\석사생\STUDY\튜토리얼 공부`에 만드는 공부 문서는 **수중 위치추정 연구를 실제로 발전시키는 지식 노트**로 제한한다.

만들어도 되는 문서:
- TOA/TDOA/DOA, UKF/Kalman, NIS/NEES, 공분산 일관성
- 수중음향 채널, 멀티패스, DOA bias, SRP-PHAT/GCC-PHAT
- carrier agility, radar glint, carrier transition, TOA branch switching
- 실패 원인 분석, 외부 문헌 연결, 새 알고리즘 후보, 독립검증 설계
- real-water 실험 데이터 계약처럼 연구 검증과 직접 연결되는 자료

만들지 않는 문서:
- 원고 버전 정리, 초록/캡션 tightening, 조판/LaTeX QA
- figure manifest, source package, submission checklist, professor package
- citation placeholder closure, BibTeX formatting, reviewer response packet처럼 논문 문서작업/마감 행정에 가까운 메모

문서작업 기록이 필요하면 numbered 작업 폴더 README에만 남기고, 공부 폴더에는 넣지 않는다.
